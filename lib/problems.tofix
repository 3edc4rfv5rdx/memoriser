== All issues from Audit Phase 1 — FIXED ==

1. FIXED: _showPhotoGallery — now accepts itemId parameter
2. FIXED: Context menu position — uses saved tap position from onTapDown
3. FIXED: Error widget — localized via lw('Error loading image')
4. FIXED: Filter status — virtual folder filters excluded from (F) indicator
5. FIXED: _checkTimeConflict — checks daily_days bitmask overlap
6. FIXED: "Debug logs" toggle fully works
7. FIXED: "Enable daily reminders" toggle now cancels/reschedules daily reminders
